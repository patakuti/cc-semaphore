{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: listens for the Rust-emitted \"snapshot\" event and calls get_snapshot once on load (02_design.md §7.3), starts a window drag from the data-tauri-drag-region header (§7.2), and resizes itself to fit its collapsed/expanded content (app.js).",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:allow-start-dragging", "core:window:allow-set-size"]
}
