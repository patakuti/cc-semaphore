[package]
name = "cc-semaphore-daemon"
version = "0.2.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "cc-semaphored"
path = "src/main.rs"

[dependencies]
cc-semaphore-core = { path = "../cc-semaphore-core" }
serde.workspace = true
serde_json.workspace = true
inotify = { version = "0.11", default-features = false }
